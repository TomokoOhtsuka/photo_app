イベントの、今までidでやりとりしていたところを、nameでやりとりする
とってくるのはparams[:id]＝URL(nameカラムのデータ)が入る
なんでparams[:id]に数字が入るかっていうと、
events/1/login
↑この「1」にあたる部分(=置き換え後は、自分で設定した任意のname)が数字であろうと、文字列であろうと、
params[:id]で取ってこられるというrailsのルールのため。

URL(=name)はイベントのnameとリンクしていて、
Photoのデータベースにある、event_idというのは、イベントのテーブルにあるidカラムとリンクしている(つまり、数字。)